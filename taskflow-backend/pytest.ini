[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    unit: Pure unit tests — no database or HTTP (deselect with -m "not unit")
    routes: Integration tests — drives the full app with an in-memory database
